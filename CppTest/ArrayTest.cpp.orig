#include <iostream>
#include <string>
using namespace std;

int main()
{
    int a[] =
    {
        1,
        2,
        3,
        10,
    };

    int n = sizeof(a)/sizeof(a[0]);
    for(int i=0; i<n; i++)
        cout<<a[i]<<endl;
    return 0;
}
